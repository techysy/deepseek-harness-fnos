[
  {
    "stepTitle": "DeepSeek Harness 配置",
    "items": [
      {
        "type": "password",
        "field": "wizard_deepseek_api_key",
        "label": "DeepSeek API Key",
        "initValue": "",
        "tip": "DeepSeek 开放平台的 API Key（sk- 开头）。可选，留空可在应用内配置。dsh 需要 DeepSeek API 才能对话。"
      },
      {
        "type": "text",
        "field": "wizard_proxy_ip",
        "label": "代理 IP（可选）",
        "initValue": "",
        "placeholder": "127.0.0.1",
        "tip": "HTTP 代理的 IP（如 mihomo/Clash 的 127.0.0.1）。如访问 GitHub 超时可填，留空默认不走代理。"
      },
      {
        "type": "text",
        "field": "wizard_proxy_port",
        "label": "代理端口（可选）",
        "initValue": "",
        "placeholder": "7890",
        "tip": "HTTP 代理的端口（如 mihomo 7890、9Router 20128）。需与代理 IP 一起填。"
      },
      {
        "type": "tips",
        "helpText": "dsh 是 DeepSeek 官方 agent 框架（DeepSeek Harness）。安装后通过 fnOS 桌面图标打开浏览器 UI（直连 http://127.0.0.1:28000），或局域网/Tailscale 直连 http://<NAS_IP>:28000。\n若访问 GitHub/外部 API 超时，可填代理 IP + 端口（如 127.0.0.1:7890），将写入 /vol4/@appdata/dsh/dsh_home/proxy.conf（PROXY=http://IP:端口）。留空默认不走代理。"
      }
    ]
  }
]
